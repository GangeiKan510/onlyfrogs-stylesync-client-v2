import { View, Text } from "react-native";
import React from "react";

const SkinToneAnalysis = () => {
  return (
    <View>
      <Text>SkinToneAnalysis</Text>
    </View>
  );
};

export default SkinToneAnalysis;
